import React, { useState, useEffect } from 'react';
import { Container, Table, Button } from 'react-bootstrap';
import { useNavigate } from 'react-router-dom';

const Cart = () => {
    const [cartItems, setCartItems] = useState([]);
    const navigate = useNavigate();

    useEffect(() => {
        fetchCartItems();
    }, []);

    const fetchCartItems = async () => {
        try {
            const response = await fetch('https://localhost:7075/api/Cart/CartByName/username'); // Replace 'username' with the actual username
            const data = await response.json();
            console.log('Cart items:', data); // Add this line
            setCartItems(data);
        } catch (error) {
            console.error('Error fetching cart items:', error);
        }
    };

    const handleRemoveFromCart = async (productId) => {
        try {
            await fetch(`https://localhost:7075/api/Cart/remove/username/${productId}`, {
                method: 'DELETE'
            });
            fetchCartItems();
        } catch (error) {
            console.error('Error removing item from cart:', error);
        }
    };

    const handleQuantityChange = async (event, productId) => {
        const newQuantity = event.target.value;
        try {
            await fetch(`https://localhost:7075/api/Cart/Update/username/${productId}`, {
                method: 'PUT',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({
                    quantity: newQuantity
                })
            });
            fetchCartItems(); // Update the cart items after successful update
        } catch (error) {
            console.error('Error updating quantity:', error);
        }
    };

    const handleBuyNow = () => {
        // Navigate to the Buy Now page
        navigate('/buynow');
    };

    return (
        <Container>
            <h2 className="my-4">Cart</h2>
            <Table striped bordered hover>
                <thead>
                    <tr>
                        <th>Product Image</th>
                        <th>Product Name</th>
                        <th>Price</th>
                        <th>Quantity</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {cartItems.map(item => (
                        <tr key={item.productId}>
                            <td><img src={`/assets/${item.imagePath}`} alt={item.productName} style={{ width: '100px', height: 'auto' }} /></td>
                            <td>{item.productName}</td>
                            <td>${item.price}</td>
                            <td>
                                <select value={item.quantity} onChange={(event) => handleQuantityChange(event, item.productId)}>
                                    {[...Array(10).keys()].map((quantity) => (
                                        <option key={quantity} value={quantity}>{quantity}</option>
                                    ))}
                                </select>
                            </td>
                            <td>
                                <Button variant="danger" onClick={() => handleRemoveFromCart(item.productId)}>Remove</Button>
                            </td>
                        </tr>
                    ))}
                </tbody>
            </Table>
            <Button variant="primary" onClick={handleBuyNow}>Buy Now</Button>
        </Container>
    );
};

export default Cart;
